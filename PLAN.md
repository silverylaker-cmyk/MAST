# 알레르기 비염 MAST 결과 → 환자 설명 슬라이드 생성기 — 개발 계획

## 1. 목표
의사가 MAST(다중 알레르기 항원 동시검사) 결과지를 웹에 업로드하면,
진료실에서 환자에게 바로 보여줄 수 있는 **환자 맞춤 설명 슬라이드**를 자동 생성한다.
슬라이드는 의사가 진료 중 넘기며 설명하는 용도이며, 인쇄/PDF로 환자에게 전달할 수도 있다.

## 2. 사용 흐름 (의사 관점)
1. 결과지 업로드 (PDF / 사진 / 스캔 이미지)
2. 자동 판독 결과 확인·수정 (항원별 Class, 양성 여부)
3. 동반질환 체크 (천식·결막염·부비동염·수면장애·후각저하) + 환자 이름·나이
4. 치료 계획 항목 확인 (기본값 미리 채워짐, 필요 시 수정)
5. 슬라이드 생성 → 진료실 화면으로 재생 / PDF 다운로드

목표 소요 시간: 업로드부터 슬라이드까지 **1분 이내**.

## 3. 슬라이드 구성 (고정 시나리오, 총 8~10장)

| # | 슬라이드 | 내용 | 데이터 출처 |
|---|---|---|---|
| 1 | 진단 확정 | "OOO님은 **알레르기 비염**으로 진단되었습니다" | 환자 정보 |
| 2 | 질병 모델 한 줄 평 (그림) | "감염이 아니라 **면역 과민반응**, 완치보다 **조절** 개념" — 감염 vs 과민반응 비교 일러스트 | 고정 |
| 3 | 내 원인 항원 | MAST 양성 항원 목록 + Class 막대 (양성 항원만 강조) | MAST 판독 |
| 4 | 계절성 / 통년성 | 항원을 계절성(꽃가루)·통년성(집먼지진드기·동물·곰팡이·바퀴)으로 나누고, 월별 캘린더 표시 | MAST 판독 + 항원 DB |
| 5 | 항원별 회피요법 | 양성 항원마다 핵심 회피법 3~4개 (카드형) | 항원 DB |
| 6 | 동반질환 확인 | 체크된 질환만 표시, 비염과의 연결 한 줄 설명 | 의사 입력 |
| 7 | 치료 계획 | 스테로이드 코스프레이 + 코세척 (기본), 경구약은 선택적 | 의사 입력(기본값) |
| 8 | 효과 시점·재진 | 효과 발현 1~2주, 재진 시기(기본 2~4주) | 의사 입력(기본값) |
| 9 | 향후 옵션 | "면역치료·수술 — 자세한 내용은 재진 때" 한 줄 | 고정 |

동반질환이 없으면 6번 생략. 양성 항원이 없으면 3~5번 대신 "음성 결과 해석" 슬라이드로 대체(2차 범위).

## 4. 기술 스택 (제안)
- **프레임워크**: Next.js (App Router, TypeScript), 단일 프로젝트로 프론트+API
- **결과지 판독**: Claude API 비전 입력 (PDF/이미지 → 항원·Class JSON 구조화 추출)
  - 파싱 결과는 항상 의사가 확인·수정하는 화면을 거침 (자동 판독은 초안일 뿐)
- **슬라이드 렌더링**: HTML/CSS 슬라이드 (키보드·클릭으로 넘김, 16:9), 브라우저 인쇄로 PDF 출력
- **데이터**: 항원 DB는 코드 내 정적 JSON (`data/allergens.json`)
- **저장**: 1차는 저장 없음 (세션 내 처리만, 개인정보 서버 보관 안 함). 결과지 파일은 판독 후 즉시 폐기
- **배포**: Vercel 또는 Docker

## 5. 항원 데이터베이스 (핵심 자산)
국내 MAST 패널(흡입성 항원 기준)에 대해 항원별로 아래 정보를 정의:
- 한글명 / 영문명 / 결과지 표기 별칭 (판독 매칭용)
- 그룹: 집먼지진드기, 동물, 곰팡이, 바퀴, 수목 꽃가루, 잔디 꽃가루, 잡초 꽃가루
- 계절성 여부 + 국내 주요 노출 월 (예: 자작나무 4~5월, 환삼덩굴 8~10월, 쑥 8~10월)
- 회피요법 문구 (환자용 쉬운 말)
- 환자용 한 줄 설명

Class 판정 기준: **Class 2 이상 양성** (기본값, 설정에서 변경 가능).

## 6. 구현 단계
1. **프로젝트 골격** — Next.js 세팅, 슬라이드 컴포넌트, 항원 DB 초안
2. **수동 입력 모드** — 판독 없이 항원 체크만으로 슬라이드 생성 (먼저 동작하는 최소 버전)
3. **결과지 업로드·자동 판독** — Claude API 연동, 판독 결과 검토 화면
4. **동반질환·치료계획 입력 폼** — 기본값 채우기
5. **PDF 출력·인쇄 최적화**
6. **디자인 다듬기** — 환자 친화적 그림, 큰 글씨, 진료실 화면용
7. (2차) 결과 저장/이력, 음식 항원 패널, 다국어

## 7. 확인 필요 사항 (결정 대기)
아래는 가정으로 진행하되, 답을 받으면 반영한다.
- 사용 중인 MAST 키트 (예: AdvanSure, PROTIA, Optigen 등) 및 결과지 실제 샘플
- 결과지 형식: PDF인지 사진인지, 검사기관 시스템 출력물인지
- 슬라이드 형식: 웹 화면 재생으로 충분한지, PPTX 파일도 필요한지
- 환자 정보 저장 여부 (저장 없음으로 가정)
- 치료 계획 기본값: 재진 시기(2~4주 가정), 특정 약제명 표기 여부
- 배포 환경 (Vercel 가정) 및 Claude API 키 보유 여부
