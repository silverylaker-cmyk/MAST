# 알레르기 비염 MAST 결과 → 환자 설명 슬라이드 생성기 — 개발 계획 (v2)

## 1. 목표
의사가 MAST 결과지 화면을 캡처해 웹 페이지에 **Ctrl+V**로 붙여넣고, 항원별 Class를 입력하면
진료실에서 환자에게 바로 넘겨 보여주는 **환자 맞춤 설명 슬라이드**가 생성된다.
결과는 구글 시트에 자동 저장되고, PDF로도 출력할 수 있다.

## 2. 사용 흐름 (의사 관점)
1. 윈도우 PC에서 결과지 화면을 `Print Screen`으로 캡처 → 웹 페이지에서 `Ctrl+V`
   - 클립보드 이미지가 그대로 화면에 붙음 (파일 저장·PDF 변환 불필요)
   - 붙인 이미지는 슬라이드 첫 부분에 "검사 결과지"로 표시됨
2. 항원 Class 입력 화면
   - MAST 전체 항원 목록이 순서대로 나열됨 (목록은 추후 제공)
   - 항원마다 0~6 버튼을 클릭해 Class 입력 (붙여넣은 결과지를 옆에 띄워 보면서 입력)
   - 기본값 0, 클릭 몇 번이면 끝
3. 환자 이름(또는 이니셜)·나이 입력 (선택)
4. "슬라이드 만들기" 클릭 → 즉시 슬라이드 재생 + 구글 시트 자동 저장
5. 필요 시 PDF 저장

> 자동 판독(OCR·AI API)은 하지 않는다. 분류·색상·회피요법 표시는 앱이 항원 DB를 기준으로 자동 처리한다.

## 3. 항원 분류 (앱이 자동 표시)
결과지 항원을 다음 **3부류**로 나눠 표시한다.

| 부류 | 하위 구분 | 예시 |
|---|---|---|
| **통년성** | — | 집먼지진드기(D.p, D.f), 개·고양이, 바퀴, 곰팡이 |
| **계절성** | 봄 / 여름 / 가을 | 봄: 자작나무·오리나무·참나무 등 수목 꽃가루 / 여름: 잔디류 / 가을: 환삼덩굴·쑥·돼지풀 |
| **교차반응·음식** | — | 음식 항원, 꽃가루-과일 교차반응 (사과·복숭아 등) |

- **양성 기준: Class 1 이상.** Class 1도 환자가 인식하도록 반드시 표시한다.
- **정도는 항원 글자 색으로 표현** (예시, 확정은 디자인 단계에서):

| Class | 색 | 의미 |
|---|---|---|
| 0 | 회색(연하게) | 음성 |
| 1 | 노랑 | 약양성 |
| 2 | 주황 | 양성 |
| 3 | 진한 주황 | 양성 |
| 4 | 빨강 | 강양성 |
| 5~6 | 진한 빨강 | 강양성 |

## 4. 슬라이드 구성 (클릭 시 한 장씩, 우상단에 ‹ 이전 / 다음 › 버튼)

| # | 슬라이드 | 내용 |
|---|---|---|
| 1 | 진단 확정 | "OOO님은 **알레르기 비염**으로 진단되었습니다" |
| 2 | 질병 모델 한 줄 평 (그림) | "감염이 아니라 **면역 과민반응**, 완치보다 **조절**" — 감염 vs 과민반응 비교 그림 |
| 3 | 검사 결과지 | 붙여넣은 캡처 이미지 |
| 4 | 내 원인 항원 | 3부류로 나눈 항원 목록, Class별 글자 색, 계절성은 봄/여름/가을 구분 + 월별 띠 |
| 5 | 항원별 회피요법 | 양성 항원(Class 1+)마다 회피법 카드 |
| 6 | 동반 질환·증상 | 천식·결막염·부비동염·수면장애·후각저하 — "이런 것이 함께 올 수 있습니다" 설명 (환자 입력 없음, 고정 내용) |
| 7 | 치료 계획 | **스테로이드 스프레이 + 코세척**(기본), **경구 알레르기약**은 선택적. 약제명 표기 없음 |
| 8 | 효과 시점·재진 | 효과 발현 1~2주, 재진 시기 |
| 9 | 향후 옵션 | "면역치료·수술 — 자세한 내용은 재진 때" 한 줄 |

조작: 화면 클릭 → 다음 장, 우상단 버튼으로 이전/다음, 키보드 ←/→ 지원, 마지막 장에서 "PDF 저장".

## 5. 기술 스택
- **정적 웹앱** (서버 없음): HTML + CSS + TypeScript, Vite 빌드
- **호스팅**: GitHub Pages (GitHub Actions로 `main` 푸시 시 자동 배포)
- **클립보드 붙여넣기**: `paste` 이벤트의 `clipboardData` 이미지 → `<img>`로 표시 (브라우저 표준 API, Edge/Chrome 지원)
- **PDF 출력**: 인쇄용 CSS(`@media print`)로 슬라이드 1장 = 1페이지 → 브라우저 "PDF로 저장"
- **구글 시트 저장**: Google Apps Script 웹앱을 시트에 붙여 배포, 앱에서 `fetch POST`로 전송
  - 저장 항목: 일시, 환자 표기, 항원별 Class, 양성 항원 요약
  - 이미지(결과지 캡처)는 시트에 저장하지 않음 (용량·개인정보). 필요 시 2차에 Drive 업로드 검토
  - 시트 URL·스크립트 URL은 설정 파일 1곳에서 관리
- **항원 DB**: `src/data/allergens.ts` — 항원명, 부류(통년/봄/여름/가을/교차·음식), 노출 월, 회피요법, 한 줄 설명

## 6. 구현 단계
1. 프로젝트 골격 (Vite + TS) + GitHub Pages 배포 워크플로
2. Ctrl+V 붙여넣기 화면 + Class 입력 폼 (임시 항원 목록으로)
3. 슬라이드 9장 + 클릭/버튼/키보드 네비게이션
4. 항원 DB 완성 (전체 목록 받은 뒤 분류·회피요법 채움)
5. PDF 인쇄 CSS
6. 구글 시트 저장 (Apps Script 연동)
7. 디자인 다듬기 (큰 글씨, 진료실 화면용, 그림)

## 7. 남은 확인 사항
- MAST 전체 항원 목록 (제공 예정)
- 재진 시기 기본값 (2~4주로 가정, 슬라이드 생성 전에 고를 수 있게 함)
- Class 입력을 항원별 버튼 클릭으로 하는 방식이 괜찮은지 (대안: 양성 항원만 골라 Class 입력)
