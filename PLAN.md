# 알레르기 비염 MAST 결과 → 환자 설명 슬라이드 생성기 — 개발 계획 (v4)

## 1. 목표
의사가 MAST 결과지 화면을 캡처해 웹 페이지에 **Ctrl+V**로 붙여넣고, 항원별 Class를 입력하면
진료실에서 환자에게 바로 넘겨 보여주는 **환자 맞춤 설명 슬라이드**가 생성된다.
결과는 구글 시트에 자동 저장되고, PDF로도 출력할 수 있다.

## 2. 사용 흐름 (의사 관점)
1. 윈도우 PC에서 결과지 화면(특이 IgE 항체 결과 표)을 `Print Screen`으로 캡처 → 웹 페이지에서 `Ctrl+V`
   - 클립보드 이미지가 그대로 화면에 붙음 (파일 저장·PDF 변환 불필요)
2. **자동 판독 (브라우저 내 OCR, 외부 API 없음)**
   - 결과지는 Class별 행에 항원 한글명이 쉼표로 나열된 요약표 형식
   - Tesseract.js(한글 모델)로 이미지 글자를 읽고, 행마다 Class 값과 항원명을 분리
   - 읽힌 항원명을 **항원 마스터 목록(165개, `data/allergens.json`)** 과 대조해 매칭 (오타·띄어쓰기 오차는 유사도 매칭으로 보정)
   - 총 IgE 값도 같이 읽음
3. **판독 확인 화면**: 인식된 항원과 Class를 표로 보여주고, 잘못 읽힌 것은 클릭으로 수정·추가·삭제
4. 환자 이름(또는 이니셜)·나이 입력 (선택)
5. "슬라이드 만들기" 클릭 → 즉시 슬라이드 재생 + 구글 시트 자동 저장
6. 필요 시 PDF 저장

> OCR은 브라우저 안에서만 돌아가며 이미지가 서버로 나가지 않는다. 결과지 형식이 항상 같으므로(표 + 쉼표 나열) 인식률이 높을 것으로 기대하지만, 확인 화면을 반드시 거친다.

## 3. 항원 분류 (앱이 자동 표시)
항원 마스터(`data/allergens.json`, 엑셀 원본 `data/MAST_antigen_list.xlsx`)에 부류를 미리 붙여 두었다.

| 부류 | 하위 구분 | 항원 수 | 포함 |
|---|---|---|---|
| **통년성** | — | 26 | 집먼지, 진드기류, 곰팡이, 동물(고양이·개·말·쥐 등), 바퀴벌레 |
| **계절성** | 봄 | 19 | 나무 꽃가루 (오리나무·자작나무·개암·참나무·플라타너스 등) |
| | 여름 | 11 | 목초 꽃가루 (버뮤다잔디·큰조아재비·호밀풀 등) |
| | 가을 | 12 | 잡초 꽃가루 (돼지풀·쑥·한삼덩굴·명아주 등) |
| **교차반응·음식** | 음식 | 83 | 계란·곡류·견과·두류·해산물·과일/채소·유제품·육류 |
| | 교차반응 | (표시) | 자작나무 관련 성분(rBet v 1 ↔ rAra h 8·rGly m 4·rCor a 1·rMal d 1·rPru p 1), CCD |
| 기타 | — | 14 | 곤충독, 라텍스, 효모, 카카오, 누에번데기, 이소시아네이트 — 양성일 때만 표시 |

- **양성 기준: Class 1 이상.** Class 1도 환자가 인식하도록 반드시 표시한다.
- 슬라이드에는 **양성(Class 1+) 항원만** 부류별로 나열한다. 음성 항원은 개수 요약도 하지 않고 아예 표시하지 않는다.
- **중복 표시 금지**: 같은 항원의 원항원·주항원·조항원(예: D. pteronyssinus, D. farinae, rDer p 10, rDer f 2 → "집먼지진드기" 하나 / Birch, rBet v 1, rBet v 2 → "자작나무" 하나)은 `family` 키로 묶어 **한 번만** 표시하고, Class는 그 묶음 중 **최고값**을 쓴다. 묶음은 부류가 같을 때만 합친다(목초 "호밀"과 식품 "호밀"은 별개).
  - 마스터에 115개 family를 정의했음 (`data/allergens.json`의 `family` 필드)
- **정도는 항원 글자 색으로 표현**:

| Class | 색 | 결과지 표기 |
|---|---|---|
| 1 | 노랑 | 낮음 |
| 2 | 주황 | 보통 |
| 3 | 진한 주황 | 보통/조금 높음 |
| 4 | 빨강 | 조금 높음 |
| 5 | 진한 빨강 | 높음 |
| 6 | 자주/검붉음 | 매우 높음 |

## 4. 슬라이드 구성 (클릭 시 한 장씩, 우상단에 ‹ 이전 / 다음 › 버튼)

| # | 슬라이드 | 내용 |
|---|---|---|
| 1 | 진단 확정 | "OOO님은 **알레르기 비염**으로 진단되었습니다" |
| 2 | 질병 모델 한 줄 평 (그림) | "감염이 아니라 **면역 과민반응**, 완치보다 **조절**" — 감염 vs 과민반응 비교 그림 |
| 3 | 검사 결과지 | 붙여넣은 캡처 이미지 |
| 4 | 내 원인 항원 | 양성 항원을 통년성 / 계절성(봄·여름·가을) / 교차반응·음식 3부류로 나열, Class별 글자 색, 총 IgE 표시 |
| 5 | 항원별 회피요법 | 양성 항원(Class 1+)마다 회피법 카드 |
| 6 | 동반 질환·증상 | 천식·결막염·부비동염·수면장애·후각저하 — "이런 것이 함께 올 수 있습니다" 설명 (환자 입력 없음, 고정 내용) |
| 7 | 치료 계획 | **스테로이드 스프레이 + 코세척**(기본), **경구 알레르기약**은 선택적. 약제명 표기 없음 |
| 8 | 효과 시점·재진 | 효과 발현 1~2주, 재진 시기 |
| 9 | 향후 옵션 | "면역치료·수술 — 자세한 내용은 재진 때" 한 줄 |

조작: 화면 클릭 → 다음 장, 우상단 버튼으로 이전/다음, 키보드 ←/→ 지원, 마지막 장에서 "PDF 저장".

## 5. 기술 스택
- **정적 웹앱** (서버 없음): React + TypeScript, Vite 빌드
- **모션 그래픽**: Remotion — 슬라이드 각 장을 Remotion 컴포지션으로 만들고 `@remotion/player`로 브라우저에서 재생
  - 장 넘길 때마다 해당 장의 애니메이션이 처음부터 재생 (글자·항원 태그가 순서대로 나타남, 꽃가루 계절 띠가 채워짐 등)
  - 같은 컴포지션을 Remotion 렌더러로 MP4 출력하는 것도 가능 (2차)
- **이미지**: 사용자가 GPT 이미지 생성으로 직접 제작 → `public/images/`에 넣음. 프롬프트는 `docs/image-prompts.md`에 정리
- **호스팅**: GitHub Pages (GitHub Actions로 `main` 푸시 시 자동 배포)
- **클립보드 붙여넣기**: `paste` 이벤트의 `clipboardData` 이미지 → `<img>`로 표시 (브라우저 표준 API, Edge/Chrome 지원)
- **PDF 출력**: 인쇄용 CSS(`@media print`)로 슬라이드 1장 = 1페이지 → 브라우저 "PDF로 저장"
- **구글 시트 저장**: Google Apps Script 웹앱을 시트에 붙여 배포, 앱에서 `fetch POST`로 전송
  - 저장 항목: 일시, 환자 표기, 항원별 Class, 양성 항원 요약
  - 이미지(결과지 캡처)는 시트에 저장하지 않음 (용량·개인정보). 필요 시 2차에 Drive 업로드 검토
  - 시트 URL·스크립트 URL은 설정 파일 1곳에서 관리
- **OCR**: Tesseract.js (한글+영문 모델, CDN 로드) — 브라우저 내 처리
- **항원 DB**: `data/allergens.json` — 165개 항원, 한글/영문명, 코드, 부류, 성분항원 여부, 교차반응 여부. 회피요법 문구는 그룹 단위로 추가 예정

## 6. 구현 단계
1. 프로젝트 골격 (Vite + React + Remotion Player) + GitHub Pages 배포 워크플로
2. Ctrl+V 붙여넣기 + OCR 판독 + 확인·수정 화면 (샘플 결과지 이미지로 인식률 검증)
3. 슬라이드 9장 + 클릭/버튼/키보드 네비게이션
4. 항원 DB 마무리 (회피요법 문구, 교차반응 설명)
5. PDF 인쇄 CSS
6. 구글 시트 저장 (Apps Script 연동)
7. 디자인·모션 다듬기 (큰 글씨, 진료실 화면용, 제작된 이미지 반영)

## 7. 남은 확인 사항
- 재진 시기 기본값 (2~4주로 가정, 슬라이드 생성 전에 고를 수 있게 함)
- 분류가 애매한 항원의 처리: 민들레(봄~가을)는 가을 잡초로, 바퀴벌레는 통년성으로 두었음
- 곤충독·라텍스·이소시아네이트 등 "기타"는 비염과 무관하므로 양성일 때만 한 줄로 표시하는 것으로 가정
